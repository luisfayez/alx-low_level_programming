#include <stdio.h>
/**
 * main-lets go there
 * Return:0, else error
 */
int main(void)
{
	int i;
	char hexvalues[i] = "0123456789abcdef";

	for (i = 0; i < 16; i++)
	{
		putchar(hexvalues[i]);
	}
	putchar('\n');
	return (0);
}
